import { useContext } from "react";
import { Context } from "../../../..";

const GenerateList = () => {
    // const {store} = useContext(Context);
    // const items = await store.generate_list();
    // console.log(items);

    return(
        <div></div>
    )
}

export default GenerateList;